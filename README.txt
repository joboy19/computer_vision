To run on the test data set:
1) Replace the master_path_to_dataset variable at the top of main.py with the path of the folder
2) Ensure that you have opencv-contrib package installed
3) Run main.py, the object detection and disparity image should be calculated and shown for each image pair
4) "p" can be pressed at any time to pause